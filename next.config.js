/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "swhschemistry.weebly.com",
            "png.pngtree.com"
        ]
    }
}

module.exports = nextConfig
