/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "swhschemistry.weebly.com",
            "img.freepik.com"
        ]
    }
}

module.exports = nextConfig
